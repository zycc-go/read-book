export type Config = {
    theme: 'dark' | 'light';
    unit: 'px' | 'rpx';
};
export const config = reactive<Config>({
    theme: "light",
    unit: 'px'
} as Config);
export const isDark = computed((): boolean => config.theme == 'dark');
export const setTheme = (theme: 'dark' | 'light') => {
    config.theme = theme;
};
//# sourceMappingURL=useConfigStore.uts.map