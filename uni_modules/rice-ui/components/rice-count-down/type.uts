
export type CountDownProps = {
	time ?: number,
	format ?: string,
	autoStart ?: boolean,
	millisecond ?: boolean,
	fontSize ?: string | number,
	color ?: string,
	textClass ?: string,
	customStyle ?: UTSJSONObject,
}